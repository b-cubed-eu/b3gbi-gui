% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/taxonomy_filter.R
\name{filter_by_taxonomy}
\alias{filter_by_taxonomy}
\title{Filter Data Cube by Taxonomy}
\description{
Filters a data cube by selected families and/or species. This is the core
filtering function used when users select taxonomic filters in the UI.
}
\usage{
filter_by_taxonomy(cube, families = NULL, species = NULL, preserve_cube = TRUE)
}
\arguments{
\item{cube}{Object of class 'processed_cube'.}
\item{families}{Character vector of family names to include. NULL or empty vector means no family filtering.}
\item{species}{Character vector of species names to include. NULL or empty vector means no species filtering.}
\item{preserve_cube}{Logical. Whether to return a full cube object (TRUE) or just the filtered data frame (FALSE). Default is TRUE.}
}
\value{
If preserve_cube is TRUE, returns a processed_cube object with filtered data. Otherwise returns a data frame.
}
\examples{
\dontrun{
cube <- load_data_cube("data.csv")

# Filter by family only
filtered <- filter_by_taxonomy(cube, families = "Pinaceae")

# Filter by species only
filtered <- filter_by_taxonomy(cube, species = "Pinus sylvestris")

# Filter by both (intersection)
filtered <- filter_by_taxonomy(
  cube,
  families = c("Pinaceae", "Rosaceae"),
  species = c("Pinus sylvestris", "Rosa canina")
)

# Get just the data frame
data <- filter_by_taxonomy(cube, families = "Pinaceae", preserve_cube = FALSE)
}
}
